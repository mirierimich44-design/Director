# Kokoro TTS (CPU-native, fast)
fastapi>=0.104.0
uvicorn>=0.24.0
kokoro-onnx>=0.4.0
soundfile>=0.12.1
numpy>=1.24.0

# Orpheus TTS (CPU via llama.cpp, slower but richer voices)
# Install separately after confirming you want it:
#   pip3 install orpheus-cpp llama-cpp-python
